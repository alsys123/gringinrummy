on Ubuntu/Mac/pc to use a server:

using python:

alias pys='python3 -m http.server 8080'

or just run:

python3 -m http.server 8080

for the subnet:

python3 -m http.server 8080 --bind 0.0.0.0

$ ifconfig | grep "10."  --> 10.0.0.199

to see it:
http://192.168.x.x:8080
